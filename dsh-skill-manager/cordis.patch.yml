# dsh-skill-manager bundle patch: 把 skill-manager 插件行插入 host 组合。
# 由 `dsh plugin --profile <name> add dsh-skill-manager` 的 reconcile 识别
# （package.json 声明 dsh.bundle），并加入 profile 的 dsh.profile.bundles 层。

- insert:
    - id: skill-manager
      name: dsh-skill-manager
